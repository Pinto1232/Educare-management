import React from 'react';
import { View } from 'react-native';

const Test: React.FC = () => {
    return (
        <View>
            {/* Component content */}
        </View>
    );
};

export default React.memo(Test);